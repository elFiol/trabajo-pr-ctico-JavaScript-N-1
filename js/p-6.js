const numeroCom = parseInt(prompt("ingrese el numero para comparar"));
const numeroCom2 = parseInt(prompt("ingrese el otro numero para comparar"));
if (numeroCom > numeroCom2){
    alert(`el numero ${numeroCom} es mayor que ${numeroCom2}`);
}else{
    alert(`el numero ${numeroCom2} es mayor que ${numeroCom}`);
};